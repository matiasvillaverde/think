import Foundation

/// Accessor for resources bundled with the `UIComponents` Swift Package target.
public enum UIComponentsResources {
    /// Resource bundle containing `UIComponents` assets.
    /// Includes `Assets.xcassets` and localized strings.
    public static let bundle: Bundle = .module
}
