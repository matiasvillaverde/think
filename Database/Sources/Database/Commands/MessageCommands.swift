import Foundation

// MARK: - Message Commands
/// Commands for managing messages in the database
public enum MessageCommands {
    // All commands are now in extension files:
    // - MessageCommands+Create.swift: Create command
    // - MessageCommands+Update.swift: UpdateResponse, UpdateProcessedOutput, UpdateToolResults
    // - MessageCommands+Read.swift: Read, GetAll, CountMessages
    // - MessageCommands+Delete.swift: Delete command
    // - MessageCommands+Errors.swift: ToolError
}
