# Changelog

All notable changes to this project will be documented in this file.

The format is based on Keep a Changelog, and this project adheres to Semantic Versioning (once versioned releases exist).

## [Unreleased]

### Added

- ThinkCLI one-command installer (`scripts/install-think-cli.sh`) and `make install` flow (`ThinkCLI/Makefile`).
- README sections for CLI install, inference modes, and documentation index.
- GitHub community templates (issue templates, PR template).

