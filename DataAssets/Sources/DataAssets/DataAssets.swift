import Foundation

/// DataAssets module
/// 
/// This module contains data assets that were previously in the Abstractions module.
/// It provides hardcoded data, configurations, and metadata that don't belong in
/// pure interface definitions.
public enum DataAssets {
    /// Module version
    public static let version = "1.0.0"
}